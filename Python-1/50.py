with open('50_4.txt','wt') as a:                                   # With function is used to save time.
       a.write("Alok is a hardworking boy")                       #It automatically closes the file after use.
                                                                   # You donot have to write a.close() while using with statement.
                               
